# 本机运行设置；不设置时使用 config.yaml 中的默认值。
LOG_LEVEL=INFO
OUTPUT_DIR=output

# 可用 CLOUDSTATION_ROOT 显式覆盖当前平台的根目录。
# CLOUDSTATION_ROOT=

# 跨平台 CloudStation 根目录；运行时会自动选择当前平台对应的值。
CLOUDSTATION_ROOT_WINDOWS=D:\CloudStaion
CLOUDSTATION_ROOT_MACOS=~/SynologyDrive/
CLOUDSTATION_ROOT_LINUX=~/CloudStation
